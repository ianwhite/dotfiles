to use this

  1. git clone git://github.com/ianwhite/dotfiles ~/.dotfiles
  2. add this following line in .bash_profile & .bashrc
      . ~/.dotfiles/profile.sh
  3. symlink other dotfiles, e.g.
      cd
      ln -s .dotfiles/.gitignore .
